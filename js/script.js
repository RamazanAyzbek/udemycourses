`use strict`; 
const num =90;
//(num===50) ? console.log(`ok !`) : console.log(`no !`); 
switch(num){
      case 39 : console.log(`no `); 
      break; 
      case 50 : console.log(`yes !`); 
      break; 
      default : console.log(`no - yes !`); 
      break; 
}